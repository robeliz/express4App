extends ../layoutUsers

block content
  h1.text-center= title
  p.text-center Editar Imagen

  div.col-md-offset-4.col-md-4
    form(action="/users/imagenes/#{image._id}?_method=PUT", method="POST" )
      div.form-group
        label(for="title") Título
        input.form-control(value="#{image.title}" type="text", id="title", name="title")
      //div.form-group
        //label(for="file") Imagen
        //input.form-control(type="file", id="file", name="file")
      //if (error!='') 
        //div.alert.alert-danger(role=alert) 
          //span.glyphicon.glyphicon-alert.margin-icon-right
          //span #{error}
      div.form-group
        button.btn.btn-info(type="sumbit", value="Guardar") Guardar